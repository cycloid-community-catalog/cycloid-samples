version: "4"
name: Caas
canonical: caas
author: "florian"
# Fill in the stack details.
description: "Allow you to deploy conteneurs as-a-service."
image: https://static.cycloid.io/docker.svg
keywords:
  - "azure"
  - "aws"
  - "digit"
  - "demo"
  - "caas"
template: false
config:
  aws:
    name: "AWS"
    cloud_provider: aws
    pipeline:
      pipeline:
        path: "pipeline/pipeline.yml"
      variables:
        path: "pipeline/variables.sample.yml"
        destination: "($ .config_root $)/pipeline/variables.yml"

  azure:
    name: "Azure"
    cloud_provider: azurerm
    pipeline:
      pipeline:
        path: "pipeline/pipeline.yml"
      variables:
        path: "pipeline/variables.sample.yml"
        destination: "($ .config_root $)/pipeline/variables.yml"
