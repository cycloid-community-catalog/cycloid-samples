# Relational database

This stack allow you to create a Relational database (Postgresql) in an existing Virtual network.

You will need to create a Virtual network using the Network stack first.

## Supported Cloud providers

- AWS
- Azure
