jobs:
- name: save-sla
  build_logs_to_retain: 3
  plan:
  - task: save-sla
    config:
        platform: linux
        image_resource:
            type: docker-image
            source: {repository: busybox}
        run:
          path: /bin/sh
          args:
          - -ec
          - |
            echo SLA saved