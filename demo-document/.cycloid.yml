version: "4"
name: Document-oriented DB
canonical: document-db
author: "florian"
# Fill in the stack details.
description: "Allow you to deploy Document-oriented DB."
image: https://static.cycloid.io/dynamodb.png
keywords:
  - "azure"
  - "aws"
  - "digit"
  - "demo"
  - "document-db"
template: false
config:
  aws:
    name: "AWS"
    cloud_provider: aws
    pipeline:
      pipeline:
        path: "pipeline/pipeline.yml"
      variables:
        path: "pipeline/variables.sample.yml"
        destination: "($ .config_root $)/pipeline/variables.yml"

  azure:
    name: "Azure"
    cloud_provider: azurerm
    pipeline:
      pipeline:
        path: "pipeline/pipeline.yml"
      variables:
        path: "pipeline/variables.sample.yml"
        destination: "($ .config_root $)/pipeline/variables.yml"
